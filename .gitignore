# Local env and secrets — never commit
.env
.env.local
.env.*.local
deploy/gcp/env.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
ENV/

# Test / tooling
.pytest_platform.sqlite3
.pytest_cache/
.coverage
htmlcov/
*.egg-info/
dist/
build/

# Optional: uncomment to ignore result JSONs (e.g. if large or sensitive)
# results/
